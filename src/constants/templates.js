export const DEFAULT_HTML = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HTML Playground</title>
</head>
<body>
  <h1>Hello, World!</h1>
</body>
</html>`;

export const DEFAULT_CSS = `body {
  font-family: sans-serif;
  margin: 20px;
  background-color: #0f172a;
  color: #f8fafc;
}`;

export const DEFAULT_WEB_JS = `console.log("Hello from Javascript!");`;

export const DEFAULT_JS = `console.log("Hello, World!");`;

export const DEFAULT_PYTHON = `print("Hello, World!")`;

export const DEFAULT_C = `#include <stdio.h>

int main() {
    printf("Hello, World!\\n");
    return 0;
}`;

export const DEFAULT_CPP = `#include <iostream>

using namespace std;

int main() {
    cout << "Hello, World!" << endl;
    return 0;
}`;

